# Ignore all files in the cell_images directory
cell_images/
cell_images.zip
# Ignore all files in the test directory
test/
# Ignore all files in the train directory
train/
# Ignore all files in the val directory
val/
# Ignore all files in the .idea directory
.idea/
# Ignore all files in the .vscode directory
.vscode/
# Ignore all files in the .DS_Store directory
.DS_Store
# Ignore all files in the .ipynb_checkpoints directory
.ipynb_checkpoints/
# Ignore all files in the .pytest_cache directory
.pytest_cache/
# Ignore all files in the .mypy_cache directory
.mypy_cache/
# Ignore all files in the .pytest_cache directory
.pytest_cache/
# Ignore all files in the .coverage directory
.coverage
# Ignore all files in the .coverage.* directory
.coverage.*
# Ignore all files in the .coverage.*.data directory
.coverage.*.data
# Ignore all files in the .coverage.*.data.json directory
.coverage.*.data.json
# Ignore all files in the .coverage.*.data.xml directory
.coverage.*.data.xml